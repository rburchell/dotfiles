#!/bin/sh
if [ "$#" -gt 0 ]; then
  ./configure -opensource -confirm-license -developer-build -DQT_MESSAGELOGCONTEXT "$@"
else
  ./configure -opensource -confirm-license -developer-build -DQT_MESSAGELOGCONTEXT
fi
