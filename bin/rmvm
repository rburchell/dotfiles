#!/bin/sh
if [ $# -lt 1 ]; then 
    echo "usage: $0 <guest>"
    echo Creates a VM.
    exit 1
fi

GUEST=$1

virsh destroy "$GUEST"
virsh undefine "$GUEST"
cryptsetup close "${GUEST}_drive" # just in case it was encrypted
lvremove "/dev/centos_static/${GUEST}_drive"
