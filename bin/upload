#!/bin/sh
if [ -z "$1" ]; then
    echo "$0: need a filename to upload"
    exit 1
fi

if ! [ -f "$1" ]; then
    echo "$0: file $1 does not exist"
    exit 1
fi

name=$(printf %q $(basename "$1"))
scp "$1" "root@http.kvm:/media/http/www/qtl.me/www/$name"
echo "uploaded https://qtl.me/$name"
