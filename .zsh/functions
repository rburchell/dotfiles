newmsgs() {
    for i in ~/Maildir/{fooishbar,collabora}/*; do 
	unset msgs
	msgs=($i/new/*(N))
        if [ ${#msgs} -gt 0 ]; then
            print ${#msgs} new messages in +$(echo $i | sed -e 's#^.*/\([^/]*/[^/]*\)#\1#;')
	fi
    done
}

zmodload zsh/pcre &>/dev/null


